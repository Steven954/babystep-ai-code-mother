# babystep-ai-code-mother

一个基于 Spring Boot、LangChain4j 和 Vue 3 的 AI 应用生成平台。

## 简介

- 支持通过自然语言生成应用
- 支持对话式修改、预览和部署
- 包含前端、后端和微服务相关代码

## 运行

后端：

```bash
./mvnw spring-boot:run
```

前端进入对应目录后执行：

```bash
npm install
npm run dev
```
